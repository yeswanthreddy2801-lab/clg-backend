import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { await prisma.college.upsert({ where: { domain: 'anits.edu.in' }, update: {}, create: { name: 'ANITS', domain: 'anits.edu.in', city: 'Visakhapatnam', status: 'active', studentCount: 5000 } }); console.log('Added ANITS to DB'); } main().catch(console.error).finally(() => prisma.$disconnect());
